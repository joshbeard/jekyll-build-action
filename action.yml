name: 'Build Jekyll'
description: 'Build a Jekyll site with Bundler and Git-safe CI setup; output is written to _site/'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'book-open'
  color: 'white'
